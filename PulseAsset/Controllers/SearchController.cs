using Microsoft.AspNetCore.Mvc;

namespace PulseAsset.Controllers;

public class SearchController
{
    // Create a new search action (get-only) that searches for Assets in the database (see AssetModel)
    // and then returns them as an array that we can parse out in the Razor view.
    
}